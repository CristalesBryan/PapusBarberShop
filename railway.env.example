# ===========================================
# VARIABLES DE ENTORNO PARA RAILWAY
# ===========================================
# Copia estas variables a tu servicio Backend en Railway
# Ve a: Tu Servicio Backend → Variables → Add Variable

# ==================== BASE DE DATOS ====================
# Conectar con el servicio PostgreSQL de Railway
# IMPORTANTE: Usa ${{ Postgres.DATABASE_URL }} para referenciar el servicio PostgreSQL
DATABASE_URL=${{ Postgres.DATABASE_URL }}

# Alternativa: Si prefieres variables individuales (no recomendado)
# DATABASE_URL=jdbc:postgresql://host:puerto/nombre_db
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=tu_password

# ==================== AWS S3 ====================
AWS_ACCESS_KEY_ID=tu_access_key_id_aqui
AWS_SECRET_ACCESS_KEY=tu_secret_access_key_aqui

# ==================== JWT ====================
# Genera una clave secreta fuerte (mínimo 256 bits)
# Puedes usar: openssl rand -base64 64
JWT_SECRET=tu_clave_secreta_jwt_muy_larga_y_aleatoria_aqui
JWT_EXPIRATION=86400000

# ==================== EMAIL (Gmail) ====================
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_contraseña_de_aplicacion_gmail

# ==================== CORS ====================
# Separa múltiples dominios con comas
# Ejemplo: https://tu-dominio.com,https://admin.tu-dominio.com
CORS_ALLOWED_ORIGINS=https://tu-dominio.com

# ==================== LOGGING (Opcional) ====================
# Niveles: DEBUG, INFO, WARN, ERROR
# En producción usar INFO o WARN
LOG_LEVEL=INFO

# ==================== HIBERNATE (Opcional) ====================
# En producción usar: validate o none
# En desarrollo usar: update
DDL_AUTO=validate

# ==================== FORMAT SQL (Opcional) ====================
# false para producción, true solo para desarrollo
FORMAT_SQL=false

# ==================== SHOW SQL (Opcional) ====================
# false para producción, true solo para desarrollo
SHOW_SQL=false

# ==================== NOTA ====================
# Railway proporciona automáticamente:
# - PORT (puerto del servidor)
# No necesitas configurarlo manualmente

